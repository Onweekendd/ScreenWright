/**
 * 计算内容哈希（仅接口占位）
 */
export function computeHash(_source: string): string {
  // no-op
  return "";
}

/**
 * 组合生成资源访问路径（仅接口占位）
 */
export function toAssetPath(_base: string, _dir: string, _file: string): string {
  // no-op
  return "";
}
