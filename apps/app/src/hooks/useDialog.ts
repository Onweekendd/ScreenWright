/**
 * @deprecated 请直接使用 /ui 导出的 useDialog
 * @example
 * import { useDialog } from "@screenwright/ui"
 */
export { type DialogOptions, type DialogProps, useDialog } from "@screenwright/ui/use-dialog";
