import { editor, optionType } from "@screenwright/material/indicator";

// 指标组件配置面板已迁移到 @screenwright/material，这里转发保持 componentOption/index.ts 聚合零改动
export { optionType as OptionTypes };
export const indicatorComponentOptions = editor;
