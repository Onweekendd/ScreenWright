import { editor } from "@screenwright/material/interactive";

// 交互组件配置面板已迁移至 @screenwright/material（editor-ui/InteractiveComponent），
// 这里仅做转发，保持 componentOption/index.ts 的聚合逻辑零改动。
export const interactiveEnumComponentOptions = editor;
