import { editor, optionType } from "@screenwright/material/media";

// 媒体组件配置面板已迁移到 @screenwright/material，这里转发保持 componentOption/index.ts 聚合零改动
export { optionType };
export const mediaComponentOptions = editor;
