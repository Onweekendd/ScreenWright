diff --git a/dist/workflow-C8ed_qpF.js b/dist/workflow-C8ed_qpF.js
index c249bbc..d8e7572 100644
--- a/dist/workflow-C8ed_qpF.js
+++ b/dist/workflow-C8ed_qpF.js
@@ -34,6 +34,36 @@ const WORKFLOW_STATUS_TO_PERSIST = [
 	"waiting"
 ];
 /**
+* Key under which the delegated (sub-agent / sub-workflow) run id rides the
+* step's own suspend payload. Namespaced so it cannot collide with the
+* executor's own suspend data.
+*/
+const DELEGATED_RUN_ID_KEY = "__mastraDelegatedRunId";
+function isPlainObject(value) {
+	return typeof value === "object" && value !== null && !Array.isArray(value);
+}
+/**
+* Attach the delegated run id to the payload handed to the workflow runtime's
+* `suspend`. Non-object payloads are left untouched — delegating tools always
+* suspend with an object or nothing, and rewrapping a primitive would change a
+* shape callers may rely on.
+*/
+function withDelegatedRunId(data, delegatedRunId) {
+	if (typeof delegatedRunId !== "string" || !delegatedRunId) return data;
+	if (data === void 0 || data === null) return { [DELEGATED_RUN_ID_KEY]: delegatedRunId };
+	if (!isPlainObject(data)) return data;
+	return {
+		...data,
+		[DELEGATED_RUN_ID_KEY]: delegatedRunId
+	};
+}
+/** Recover the delegated run id from the step's persisted suspend payload. */
+function readDelegatedRunId(suspendData) {
+	if (!isPlainObject(suspendData)) return void 0;
+	const value = suspendData[DELEGATED_RUN_ID_KEY];
+	return typeof value === "string" && value ? value : void 0;
+}
+/**
 * Builds the per-task workflow that owns executor + retries.
 *
 * Uses the standard (default) execution engine so the workflow runs entirely
@@ -57,7 +87,7 @@ function buildBackgroundTaskWorkflow(manager) {
 		id: "run-attempt",
 		inputSchema: bodyIOSchema,
 		outputSchema: attemptOutputSchema,
-		execute: async ({ inputData, abortSignal: workflowAbortSignal, suspend, resumeData }) => {
+		execute: async ({ inputData, abortSignal: workflowAbortSignal, suspend, resumeData, suspendData }) => {
 			const { taskId } = inputData;
 			const storage = await manager.getStorage();
 			const task = await storage.getTask(taskId);
@@ -129,14 +159,19 @@ function buildBackgroundTaskWorkflow(manager) {
 					suspendOptions
 				};
 			};
+			const delegatedRunId = readDelegatedRunId(suspendData);
+			const execArgs = resumeData !== void 0 && delegatedRunId ? {
+				...task.args,
+				suspendedToolRunId: delegatedRunId
+			} : task.args;
 			try {
-				const result = await executor.execute(task.args, {
+				const result = await executor.execute(execArgs, {
 					abortSignal: abortController.signal,
 					onProgress,
 					suspend: wrappedSuspend,
 					resumeData
 				});
-				if (pendingSuspend) return suspend(pendingSuspend.data, pendingSuspend.suspendOptions);
+				if (pendingSuspend) return suspend(withDelegatedRunId(pendingSuspend.data, pendingSuspend.suspendOptions?.runId), pendingSuspend.suspendOptions);
 				return {
 					taskId,
 					outcome: "success",
diff --git a/dist/workflow-CRgUhwoZ.cjs b/dist/workflow-CRgUhwoZ.cjs
index 18bf108..496ebc7 100644
--- a/dist/workflow-CRgUhwoZ.cjs
+++ b/dist/workflow-CRgUhwoZ.cjs
@@ -34,6 +34,36 @@ const WORKFLOW_STATUS_TO_PERSIST = [
 	"waiting"
 ];
 /**
+* Key under which the delegated (sub-agent / sub-workflow) run id rides the
+* step's own suspend payload. Namespaced so it cannot collide with the
+* executor's own suspend data.
+*/
+const DELEGATED_RUN_ID_KEY = "__mastraDelegatedRunId";
+function isPlainObject(value) {
+	return typeof value === "object" && value !== null && !Array.isArray(value);
+}
+/**
+* Attach the delegated run id to the payload handed to the workflow runtime's
+* `suspend`. Non-object payloads are left untouched — delegating tools always
+* suspend with an object or nothing, and rewrapping a primitive would change a
+* shape callers may rely on.
+*/
+function withDelegatedRunId(data, delegatedRunId) {
+	if (typeof delegatedRunId !== "string" || !delegatedRunId) return data;
+	if (data === void 0 || data === null) return { [DELEGATED_RUN_ID_KEY]: delegatedRunId };
+	if (!isPlainObject(data)) return data;
+	return {
+		...data,
+		[DELEGATED_RUN_ID_KEY]: delegatedRunId
+	};
+}
+/** Recover the delegated run id from the step's persisted suspend payload. */
+function readDelegatedRunId(suspendData) {
+	if (!isPlainObject(suspendData)) return void 0;
+	const value = suspendData[DELEGATED_RUN_ID_KEY];
+	return typeof value === "string" && value ? value : void 0;
+}
+/**
 * Builds the per-task workflow that owns executor + retries.
 *
 * Uses the standard (default) execution engine so the workflow runs entirely
@@ -57,7 +87,7 @@ function buildBackgroundTaskWorkflow(manager) {
 		id: "run-attempt",
 		inputSchema: bodyIOSchema,
 		outputSchema: attemptOutputSchema,
-		execute: async ({ inputData, abortSignal: workflowAbortSignal, suspend, resumeData }) => {
+		execute: async ({ inputData, abortSignal: workflowAbortSignal, suspend, resumeData, suspendData }) => {
 			const { taskId } = inputData;
 			const storage = await manager.getStorage();
 			const task = await storage.getTask(taskId);
@@ -129,14 +159,19 @@ function buildBackgroundTaskWorkflow(manager) {
 					suspendOptions
 				};
 			};
+			const delegatedRunId = readDelegatedRunId(suspendData);
+			const execArgs = resumeData !== void 0 && delegatedRunId ? {
+				...task.args,
+				suspendedToolRunId: delegatedRunId
+			} : task.args;
 			try {
-				const result = await executor.execute(task.args, {
+				const result = await executor.execute(execArgs, {
 					abortSignal: abortController.signal,
 					onProgress,
 					suspend: wrappedSuspend,
 					resumeData
 				});
-				if (pendingSuspend) return suspend(pendingSuspend.data, pendingSuspend.suspendOptions);
+				if (pendingSuspend) return suspend(withDelegatedRunId(pendingSuspend.data, pendingSuspend.suspendOptions?.runId), pendingSuspend.suspendOptions);
 				return {
 					taskId,
 					outcome: "success",
diff --git a/dist/agent-CKAVuxKN.js b/dist/agent-CKAVuxKN.js
--- a/dist/agent-CKAVuxKN.js
+++ b/dist/agent-CKAVuxKN.js
@@ -11,7 +11,7 @@
 import { isStandardSchemaWithJSON, standardSchemaToJSONSchema, toStandardSchema } from "./schema/index.js";
 import { n as Tool, r as createTool } from "./tool-BogyE8Hu.js";
 import { i as MastraFGAPermissions } from "./ee-DXvSoTl7-BqTsKmQd.js";
-import { a as createBackgroundTask, i as resolveBackgroundConfig, t as generateBackgroundTaskSystemPrompt } from "./background-tasks-C4VvnJ3T.js";
+import { a as createBackgroundTask, i as resolveBackgroundConfig, t as generateBackgroundTaskSystemPrompt, u as resolveAgentToolConfig } from "./background-tasks-C4VvnJ3T.js";
 import { a as isProviderTool, i as isProviderDefinedTool, n as getProviderToolName, r as isMastraTool, t as getNeedsApprovalFn } from "./toolchecks-BsyK_wBH.js";
 import { n as ToolStream, t as noopObserve } from "./types-C59tsW89.js";
 import { a as ensureToolProperties, b as selectFields, d as makeCoreTool, i as delay, n as createMastraProxy, o as generateEmptyFromSchema, r as deepMerge$1 } from "./utils-DbzrFK9f.js";
@@ -31213,7 +31213,7 @@
 		agentSpan,
 		methodType,
 		memory,
-		backgroundTaskEnabled: backgroundTaskManager?.config?.enabled,
+		backgroundTaskEnabled: backgroundTaskManager?.config?.enabled ? (toolName) => typeof toolName === "string" && resolveAgentToolConfig(toolName, agentBackgroundConfig)?.enabled === true : false,
 		runScope
 	});
 	const prepareMemoryStep = createPrepareMemoryStep({
@@ -34695,7 +34695,7 @@
 				additionalProperties: true
 			} };
 			const inputRequired = ["inputData"];
-			if (workflow.stateSchema) inputProperties.initialState = standardSchemaToJSONSchema(workflow.stateSchema, { io: "input" });
+			if (workflow.stateSchema && workflow.exposeInitialStateToAgent) inputProperties.initialState = standardSchemaToJSONSchema(workflow.stateSchema, { io: "input" });
 			const extendedInputSchema = {
 				type: "object",
 				properties: inputProperties,
diff --git a/dist/utils-DbzrFK9f.js b/dist/utils-DbzrFK9f.js
--- a/dist/utils-DbzrFK9f.js
+++ b/dist/utils-DbzrFK9f.js
@@ -145,7 +145,7 @@
 		this.originalTool = input.originalTool;
 		this.options = input.options;
 		this.logType = input.logType;
-		const isBackgroundEligible = !!input.backgroundTaskEnabled;
+		const isBackgroundEligible = typeof input.backgroundTaskEnabled === "function" ? input.backgroundTaskEnabled(this.originalTool.id) : !!input.backgroundTaskEnabled;
 		const isResumableTool = input.autoResumeSuspendedTools || this.originalTool.id?.startsWith("agent-") || this.originalTool.id?.startsWith("workflow-");
 		if (!isVercelTool(this.originalTool) && !isProviderDefinedTool(this.originalTool)) {
 			if (isBackgroundEligible || isResumableTool) {
diff --git a/dist/background-tasks-C4VvnJ3T.js b/dist/background-tasks-C4VvnJ3T.js
--- a/dist/background-tasks-C4VvnJ3T.js
+++ b/dist/background-tasks-C4VvnJ3T.js
@@ -1158,7 +1158,8 @@
 	const eligibleTools = [];
 	const enableAll = agentConfig?.tools === "all";
 	for (const [toolName, tool] of Object.entries(tools)) {
-		const bgEnabledFromAgentConfig = agentConfig?.tools === "all" ? false : typeof agentConfig?.tools?.[toolName] === "boolean" ? agentConfig.tools[toolName] : agentConfig?.tools?.[toolName]?.enabled ?? false;
+		const bgEnabledFromAgentConfig = agentConfig?.tools === "all" ? false : resolveAgentToolConfig(toolName, agentConfig)?.enabled ?? false;
+		if (!enableAll && resolveAgentToolConfig(toolName, agentConfig) === void 0 && !tool.background) continue;
 		eligibleTools.push({
 			toolName,
 			toolConfig: tool.background,
@@ -1183,6 +1184,6 @@
 IMPORTANT: "_background" field is always an object. The fields in the _background field should be inside the _background object, not outside of it.`;
 }
 //#endregion
-export { createBackgroundTask as a, resolveBackgroundConfig as i, backgroundOverrideJsonSchema as n, BackgroundTaskManager as o, backgroundOverrideZodSchema as r, BACKGROUND_TASK_WORKFLOW_ID as s, generateBackgroundTaskSystemPrompt as t };
+export { createBackgroundTask as a, resolveBackgroundConfig as i, backgroundOverrideJsonSchema as n, BackgroundTaskManager as o, backgroundOverrideZodSchema as r, BACKGROUND_TASK_WORKFLOW_ID as s, generateBackgroundTaskSystemPrompt as t, resolveAgentToolConfig as u };
 
 //# sourceMappingURL=background-tasks-C4VvnJ3T.js.map
diff --git a/dist/agent-BOdDHE12.cjs b/dist/agent-BOdDHE12.cjs
--- a/dist/agent-BOdDHE12.cjs
+++ b/dist/agent-BOdDHE12.cjs
@@ -31215,7 +31215,7 @@
 		agentSpan,
 		methodType,
 		memory,
-		backgroundTaskEnabled: backgroundTaskManager?.config?.enabled,
+		backgroundTaskEnabled: backgroundTaskManager?.config?.enabled ? (toolName) => typeof toolName === "string" && require_background_tasks.resolveAgentToolConfig(toolName, agentBackgroundConfig)?.enabled === true : false,
 		runScope
 	});
 	const prepareMemoryStep = createPrepareMemoryStep({
@@ -34697,7 +34697,7 @@
 				additionalProperties: true
 			} };
 			const inputRequired = ["inputData"];
-			if (workflow.stateSchema) inputProperties.initialState = (0, _mastra_schema_compat_schema.standardSchemaToJSONSchema)(workflow.stateSchema, { io: "input" });
+			if (workflow.stateSchema && workflow.exposeInitialStateToAgent) inputProperties.initialState = (0, _mastra_schema_compat_schema.standardSchemaToJSONSchema)(workflow.stateSchema, { io: "input" });
 			const extendedInputSchema = {
 				type: "object",
 				properties: inputProperties,
diff --git a/dist/utils-DrEm8BrC.cjs b/dist/utils-DrEm8BrC.cjs
--- a/dist/utils-DrEm8BrC.cjs
+++ b/dist/utils-DrEm8BrC.cjs
@@ -145,7 +145,7 @@
 		this.originalTool = input.originalTool;
 		this.options = input.options;
 		this.logType = input.logType;
-		const isBackgroundEligible = !!input.backgroundTaskEnabled;
+		const isBackgroundEligible = typeof input.backgroundTaskEnabled === "function" ? input.backgroundTaskEnabled(this.originalTool.id) : !!input.backgroundTaskEnabled;
 		const isResumableTool = input.autoResumeSuspendedTools || this.originalTool.id?.startsWith("agent-") || this.originalTool.id?.startsWith("workflow-");
 		if (!require_toolchecks.isVercelTool(this.originalTool) && !require_toolchecks.isProviderDefinedTool(this.originalTool)) {
 			if (isBackgroundEligible || isResumableTool) {
diff --git a/dist/background-tasks-BOCbbAxn.cjs b/dist/background-tasks-BOCbbAxn.cjs
--- a/dist/background-tasks-BOCbbAxn.cjs
+++ b/dist/background-tasks-BOCbbAxn.cjs
@@ -1158,7 +1158,8 @@
 	const eligibleTools = [];
 	const enableAll = agentConfig?.tools === "all";
 	for (const [toolName, tool] of Object.entries(tools)) {
-		const bgEnabledFromAgentConfig = agentConfig?.tools === "all" ? false : typeof agentConfig?.tools?.[toolName] === "boolean" ? agentConfig.tools[toolName] : agentConfig?.tools?.[toolName]?.enabled ?? false;
+		const bgEnabledFromAgentConfig = agentConfig?.tools === "all" ? false : resolveAgentToolConfig(toolName, agentConfig)?.enabled ?? false;
+		if (!enableAll && resolveAgentToolConfig(toolName, agentConfig) === void 0 && !tool.background) continue;
 		eligibleTools.push({
 			toolName,
 			toolConfig: tool.background,
@@ -1225,5 +1226,11 @@
 		return resolveBackgroundConfig;
 	}
 });
+Object.defineProperty(exports, "resolveAgentToolConfig", {
+	enumerable: true,
+	get: function() {
+		return resolveAgentToolConfig;
+	}
+});
 
 //# sourceMappingURL=background-tasks-BOCbbAxn.cjs.map
