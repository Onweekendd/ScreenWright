# setAnimationPlay / setAnimationPause — 播放/暂停关键帧动画

控制目标组件的关键帧动画播放或暂停。

## 字段

| 字段路径 | 类型 | 说明 |
|---------|------|------|
| `keyframesName` | string | 关键帧动画名称 |
| `keyframesPlayDelay` | number (ms) | 关键帧播放延迟（毫秒） |
