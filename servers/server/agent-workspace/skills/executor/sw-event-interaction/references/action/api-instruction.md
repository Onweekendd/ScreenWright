# handleApiInstruction — API 指令集

向场景发送 API 指令。

## 字段

| 字段路径 | 类型 | 说明 |
|---------|------|------|
| `apiInstructionDetail` | string | API指令详情（指令内容） |
| `apiInstructionDelay` | number (ms) | API指令延迟（毫秒） |
