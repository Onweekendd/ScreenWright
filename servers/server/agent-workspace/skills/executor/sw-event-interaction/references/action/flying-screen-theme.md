# SwitchFlyingScreenTheme — 切换飞屏主题

切换大屏的飞屏显示主题。

## 字段

| 字段路径 | 类型 | 说明 |
|---------|------|------|
| `screenThemeId` | number | 飞屏主题ID（通过 getFlyingScreenPageGroup 获取） |
| `screenThemePages` | any[] | 飞屏主题页面集合 |
