# followIcon / focusLayer — 跟随图标 / 聚焦倾斜部件

- `followIcon`：场景中的跟随图标行为
- `focusLayer`：聚焦倾斜部件

两者均仅需设置延迟。

## 字段

| 字段路径 | 类型 | 说明 |
|---------|------|------|
| `animation.delay` | number (ms) | 动画延迟时间（毫秒） |
