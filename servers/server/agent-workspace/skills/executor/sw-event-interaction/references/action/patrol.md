# 轮巡控制

以下轮巡控制操作均只需设置 `animation.delay`（动画延迟时间，毫秒）：

- `turnOnPatrol` — 开启轮巡
- `pausePatrol` — 暂停轮巡
- `restartPatrol` — 重启轮巡

## 字段

| 字段路径 | 类型 | 说明 |
|---------|------|------|
| `animation.delay` | number (ms) | 动画延迟时间（毫秒） |
