# switchSceneObjVisible — 切换场景对象显隐

控制 3D 场景中指定对象的显示或隐藏。

## 字段

| 字段路径 | 类型 | 说明 |
|---------|------|------|
| `sceneObject.nameList` | string[] | 对象名称列表 |
| `sceneObject.objInfoList` | any[] | 对象信息列表 |
| `sceneObject.visible` | string | 可见性状态：`"show"` / `"hide"` / `"switch"`（显隐切换） |
