# switchSceneStatus — 切换场景状态

适用于 `threescene` / `industryscene` 组件，切换到指定场景状态。

## 字段

| 字段路径 | 类型 | 说明 |
|---------|------|------|
| `sceneStatusName` | string | 场景状态名称（目标状态名称） |
| `switchSceneStatusDelay` | number (ms) | 场景状态切换延迟（毫秒） |
