# setIndex — 设置选中项

适用于 `subtabs` / `multiSubtabs` / `rollSubtabs` / `ft-customselect` / `scrollPicker` / `iot-subTabs` 组件，设置当前选中项的索引。

## 字段

| 字段路径 | 类型 | 说明 |
|---------|------|------|
| `animation.idxValue` | number \| string | 索引值（目标选中项的索引） |
| `animation.delay` | number (ms) | 动画延迟时间（毫秒） |
