# 签名板操作

以下签名板操作均只需设置 `animation.delay`（动画延迟时间，毫秒）：

- `onExport` — 导出签名
- `onClear` — 清除签名
- `onRedo` — 重做
- `onUndo` — 撤销
- `signature` — 签名

## 字段

| 字段路径 | 类型 | 说明 |
|---------|------|------|
| `animation.delay` | number (ms) | 动画延迟时间（毫秒） |
