# setStateAnimationPlay — 播放状态动画

播放指定组件的状态动画。

## 字段

| 字段路径 | 类型 | 说明 |
|---------|------|------|
| `stateAnimationName` | string | 状态动画名称 |
| `animationState` | number | 动画状态 |
| `stateAnimationPlayDelay` | number (ms) | 状态动画播放延迟（毫秒） |
