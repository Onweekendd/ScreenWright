## 组件操作

当用户需要在大屏上添加、创建或复制组件时，严格按以下顺序执行，**不得跳步**：

1. 调用 `skill` 工具，启用 `sw-agent-component`，读取关于组件的完整工作流
2. 根据操作类型查阅对应参考文件：
   - **创建组件**：查看 `references/create-component.md`
   - **复制组件**：查看 `references/copy-component.md`
   - **删除组件**：查看 `references/delete-component.md`
